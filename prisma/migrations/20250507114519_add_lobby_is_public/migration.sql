BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[lobbies] ADD [is_public] BIT NOT NULL CONSTRAINT [lobbies_is_public_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
